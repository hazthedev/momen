/** @type {import('next').NextConfig} */
const nextConfig = {
  // Image optimization configuration for Next.js 15
  images: {
    // Use custom loader for R2 images
    loader: 'custom',
    loaderFile: './lib/storage/image-loader.ts',
    // Remote patterns for R2 and other sources
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**.r2.dev',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: '**.r2.cloudflarestorage.com',
        port: '',
        pathname: '/**',
      },
    ],
    // Unstable but required for custom loaders in Next.js 15
    unoptimized: false,
  },

  // Logging configuration
  logging: {
    fetches: {
      fullUrl: true,
    },
  },

  // Environment variables available to the browser
  env: {
    APP_URL: process.env.APP_URL,
  },

  // Production optimizations
  productionBrowserSourceMaps: false,

  // Compression
  compress: true,

  // Power optimizations for Next.js 15
  poweredByHeader: false,

  // Experimental features that are actually stable in Next.js 15
  experimental: {
    // Enable optimized package imports
    optimizePackageImports: ['lucide-react', '@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu'],
  },

  // Webpack configuration - simplified for Next.js 15
  webpack: (config, { isServer }) => {
    // Next.js 15 has better polyfill handling, but we keep this for compatibility
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        fs: false,
        net: false,
        tls: false,
        crypto: false,
        stream: false,
      };
    }

    // Ensure proper module resolution
    config.resolve.extensionAlias = {
      '.js': ['.ts', '.tsx', '.js', '.jsx'],
    };

    return config;
  },

  // Output configuration for better build manifest generation
  output: 'standalone',

  // Ensure proper build asset handling
  generateBuildId: async () => {
    return 'build-' + Date.now();
  },
};

export default nextConfig;
