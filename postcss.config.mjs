/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    '@tailwindcss/forms': {},
    '@tailwindcss/typography': {},
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
